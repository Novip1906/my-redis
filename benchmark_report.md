# Отчет о бенчмарке форматов AOF (Append Only File)

## Среда тестирования

| Параметр | Значение |
| :--- | :--- |
| CPU | Apple M1 Pro |
| RAM | 16 GB |
| OS | macOS 26.3.1 |
| Go | 1.25.1 darwin/arm64 |

## Методология

Бенчмарк выполняет **последовательную запись 1 000 000** команд из набора разнообразных строк (ASCII, кириллица, длинные строки, несколько аргументов) в файл для каждого из 4 форматов сериализации. Запись идёт синхронно, без конкурентности, чтобы измерить чистую производительность сериализации и записи в файл. По окончании записи вызывается `file.Sync()` для гарантии сброса буферов на диск.


## Результаты тестирования (1 000 000 записей)

| Формат | Запись (сек) | Чтение/AOF Restore (мс) | Размер файла (МБ) |
| :--- | :--- | :--- | :--- |
| **Binary** | 3.65 | **46** | 36.8 |
| **Text** | **3.47** | **43** | **33.8** |
| **JSON** | 3.69 | 530 | 44.2 |
| **RESP** | 4.03 | 238 | 51.4 |

*Примечание: Чтение проводилось сразу после записи. Оптимизация десериализации RESP позволила сократить время чтения в 2 раза (с ~485мс до ~238мс).*

---

## Анализ форматов

### 1. Text (Простой текст)
*   **Плюсы:** Максимально компактный размер (всего +1 байт `\n` на команду). Идеальная читаемость.
*   **Минусы:** **Ненадежность.** Текущий парсер сломается, если значение содержит перенос строки. Чтение медленнее бинарного из-за необходимости постоянного поиска разделителя `\n` в потоке.

### 2. Binary (Бинарный)
*   **Плюсы:** **Максимальная производительность при чтении.** `ReadFull` по префиксу длины работает быстрее, чем сканирование текста. **Надежность:** корректно сохраняет любые бинарные данные без экранирования.
*   **Минусы:** Нечитаем вручную. Всегда добавляет 4 байта (uint32) на каждую команду, что на коротких записях дает +10-15% к размеру по сравнению с текстом.

### 3. JSON
*   **Плюсы:** Структурированность, поддержка любыми языками, читаемость.
*   **Минусы:** Избыточность (повторяющиеся ключи `"cmd":`). Самый высокий CPU overhead из-за работы `json.Marshal/Unmarshal` через рефлексию. 

### 4. RESP (Redis Serialization Protocol)
*   **Плюсы:** **Промышленный стандарт.** Поддерживает произвольные бинарные данные ("safe"), структурирован как массив аргументов.
*   **Минусы:** **Крайне избыточен.** Служебные символы `*`, `$`, `\r\n` увеличивают размер файла почти в 1.8 раза по сравнению с текстом.

## Итоговая рекомендация
Для реализации собственного Redis-подобного хранилища **бинарный формат** является лучшим выбором по балансу скорости восстановления и надежности данных. **Text** формат стоит оставить только для отладки, так как он требует сложного экранирования для реальных данных. **RESP** следует использовать только если важна прямая совместимость с клиентами Redis.
